networks:
  headsntails-mesh:
    driver: bridge

volumes:
  postgres_data:
    driver: local

services:
  postgres:
    image: postgres:15-alpine
    container_name: headsntails-postgres
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - headsntails-mesh
    ports:
      - "${POSTGRES_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 3s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: headsntails-redis
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    networks:
      - headsntails-mesh
    ports:
      - "${REDIS_PORT}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5

  headsntails-core:
    container_name: headsntails-core
    build:
      context: ../headsntails-core
      dockerfile: Dockerfile
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=${POSTGRES_USER}
      - DB_PASS=${POSTGRES_PASSWORD}

      - RATE_LIMITER_URL=${RATE_LIMITER_INTERNAL_URL}
      - JWT_SECRET_KEY=${LOCAL_JWT_SECRET}
      - RATE_LIMITER_GRPC_URL=rate-limiter:50051
      - RATE_LIMITER_GRPC_CALL=${RATE_LIMITER_GRPC_CALL}

      # =========================================================================
      # REDIS ALIGNMENT MATRIX
      # =========================================================================
      - REDIS_ADDR=redis:6379
      - REDIS_PASSWORD=
    ports:
      - "8080:8080"
    networks:
      - headsntails-mesh
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  rate-limiter:
    container_name: headsntails-rate-limiter
    build:
      context: ../rate-limiter
      dockerfile: Dockerfile
    command: >
      sh -c "pip install -r requirements.txt && 
             uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4"
    environment:
      - REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/0
    ports:
      - "8000:8000"
      - "50051:50051"
    networks:
      - headsntails-mesh
    depends_on:
      redis:
        condition: service_healthy

  jwt-authority:
    container_name: headsntails-jwt-authority
    build:
      context: ../jwt-authority
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      - JWT_SECRET_KEY=${LOCAL_JWT_SECRET}
    ports:
      - "3000:3000"
    networks:
      - headsntails-mesh

  nginx-gateway:
    image: nginx:1.25-alpine
    container_name: nginx-gateway
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - headsntails-mesh
    depends_on:
      - headsntails-core
      - jwt-authority
    restart: always